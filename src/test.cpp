

class TestInterface
{
  virtual printfunc() = 0;
};


class TestDerived : TestInterface
{
  printfunc() override
  {
    // ....
  }
};


TestDerived obj;
TestInterface obj2;

int arr[] = { 1, 2, 3, 3, 4, 4, 7, 7 ,7, 8 }
int new_size = dedup(arr, 10);

int dedup(int arr[], int len)
{
  for (size_t i = 1; i < len; ++i)
  {
    j 
    if(arr[i]!=arr[i-1]) 
  }
}